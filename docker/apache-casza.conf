# Permite que funcione el .htaccess del proyecto
<Directory /var/www/html>
    AllowOverride All
    Require all granted
</Directory>
# Render atiende HTTPS y reenvía por HTTP: se marca la conexión como segura
SetEnvIf X-Forwarded-Proto "^https$" HTTPS=on
ServerName localhost
